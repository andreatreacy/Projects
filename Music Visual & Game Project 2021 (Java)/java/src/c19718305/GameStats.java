package c19718305;

// An Interface has no fields or method bodies just method signatures
// any class that implements an interface needs to use the interface's methods

public interface GameStats {
    void updateStats(Game g);
}